
use crate::runtime::interpreter::Interpreter;



pub fn register_string_words(_interpreter: &mut dyn Interpreter)
{
    /* add_native_word!(interpreter, "hex", word_hex,
        "Convert a number into a hex string.",
        "number -- hex_string"); */
}


use crate::runtime::{ error,
                      interpreter::Interpreter };



pub fn word_term_raw_mode(_interpreter: &mut dyn Interpreter) -> error::Result<()>
{
    Ok(())
}

pub fn word_term_size(_interpreter: &mut dyn Interpreter) -> error::Result<()>
{
    Ok(())
}

pub fn word_term_key(_interpreter: &mut dyn Interpreter) -> error::Result<()>
{
    Ok(())
}
